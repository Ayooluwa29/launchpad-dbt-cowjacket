version: 2

models:
  - name: customer_loyalty
    description: "customer loyalty table"
    columns:
      - name: total_points_earned
      - name: last_activity_date
      - name: total_loyalty_transactions