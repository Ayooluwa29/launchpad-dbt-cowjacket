version: 2

models:
  - name: "fact_customer_orders"
    description: "customer order table in marts"
    columns:
      - name: "customer_id"
      - name: "first_order_date"
      - name: "last_order_date"
      - name: "total_orders"
      - name: "lifetime_value"